import Link from "next/link";
import { AdminDocumentEditor } from "@/components/admin-document-editor";
import { requireAdminPage } from "@/lib/server/admin";
import { requireDatabase } from "@/lib/server/db";

export default async function AdminServicesPage({ searchParams }: { searchParams: Promise<{ edit?: string; new?: string }> }) { await requireAdminPage(); const db = requireDatabase(); const params = await searchParams; const items = await db.contentDocument.findMany({ where: { kind: "SERVICE" }, include: { revisions: { orderBy: { version: "desc" }, take: 1 } }, orderBy: { createdAt: "asc" } }); const selected = params.edit ? items.find((item) => item.id === params.edit) : undefined; return <><div className="admin-heading"><div><p className="admin-kicker">CONTENT / SERVICES</p><h2>Layanan</h2></div><Link className="button button-light" href="/admin/services?new=1">Tambah layanan</Link></div><div className="admin-table">{items.map((item) => <Link key={item.id} href={`/admin/services?edit=${item.id}`}><span>{(item.revisions[0]?.payload as { title?: string })?.title ?? item.slug}</span><span>{item.revisions[0]?.status}</span></Link>)}</div>{selected || params.new ? <AdminDocumentEditor kind="SERVICE" slug={selected?.slug ?? `service-${items.length + 1}`} documentId={selected?.id} initial={selected?.revisions[0]?.payload as Record<string, unknown> | undefined} /> : <p className="admin-empty">Pilih layanan untuk diedit atau tambahkan layanan baru.</p>}</>; }
